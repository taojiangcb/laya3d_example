{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildclient",
            "command": "gulp",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "problemMatcher": []
        }
    ]
}